#include <bits/stdc++.h>

using std :: cin; 
using std :: cout; 

int main() {
    std :: vector < int > a; 
    for(int i = 0; i < 10; ++i) a.push_back(i); 
    for(auto &x : a) cout << x << ' '; 
    return 0; 
}